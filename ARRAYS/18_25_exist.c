// Check if the number 25 exists in an array of 8 integers.
#include <stdio.h>
int main()
{
    int arr[8], i, num, n;

    for (i = 0; i < 8; i++)
    {
        printf("Enter Array [%d] = ", i);
        scanf("%d", &num);
    }
    num=n;
    
        if (n == 25)
        {
            printf("25 existed");
        }
        else
        {
            printf("25 not Existed");
        }
    

    return 0;
}