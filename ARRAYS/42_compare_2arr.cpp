// Compare two arrays and count how many elements are the same at the same positions.
#include <iostream>
using namespace std;
int main()
{
    int arr1[1,2,3];
    int arr2[1,3,5]; 
    int i;

    
    return 0;
}