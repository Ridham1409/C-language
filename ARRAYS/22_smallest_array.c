// Find the smallest number in an array of 5 integers.
#include <stdio.h>
int main()
{
    int arr[5], i, min

                       printf("Enter 5 Arrays \n");
 for (i = o; i >= 0; i ==)
    {
        if (arr[i] < min)
        {
            min = arr[i];
            printf("Smallest number is %d\n", min);
        }
    }
    return 0;
}